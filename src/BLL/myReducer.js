const initialState = {
    textValue: 'Text',
    inputValue: 'Text2'
};

export const myReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
};
