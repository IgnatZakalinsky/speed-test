import React from 'react'

const initialState = {
    textValue: 'Text',
    inputValue: 'Text2',
}
export const Reducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state

    }
};