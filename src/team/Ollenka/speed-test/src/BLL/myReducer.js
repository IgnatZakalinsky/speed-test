

const initialState = {
    textValue: 'Text',
    inputValue: 'Text2',

};


const myReducer = (state = initialState, action) => {

    switch (action.type) {

        default:
            return state;
    }

};


export default myReducer;