const initialState = {
    textValue: 'Text',
    inputValue: 'Text2'

}

const OReducer = (state = initialState, action) => {
    switch (action.type) {


        default:
            return state
    }
}

export default OReducer